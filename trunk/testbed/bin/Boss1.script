controller Boss1
{
	// emitters - all these values are offsets from controller values
	left = emitter Boss1Gun1(0, 0, 0);

	// states
	Stage1 = state
	{
		wait(10);
	}
}

emitter Boss1Gun1
{
	Emit = state
	{
		fireA bullet(180, 100);
		wait(1);
	}
}

